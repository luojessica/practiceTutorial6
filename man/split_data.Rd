% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_data.R
\name{split_data}
\alias{split_data}
\title{Split Data into Training and Testing Sets}
\usage{
split_data(data, seed = 123)
}
\arguments{
\item{data}{A data frame containing a \code{species} column for stratification.}

\item{seed}{An integer used to set the random seed for reproducibility. Default is 123.}
}
\value{
A list with two elements: \code{train} and \code{test}, each a data frame.
}
\description{
This function splits a data frame into training and testing sets using stratified sampling on the \code{species} column.
}
\examples{
library(palmerpenguins)
data <- na.omit(penguins)
data <- prepare_data(data)
splits <- split_data(data)
train <- splits$train
test <- splits$test
}
